/// <reference types="vite/client" />

// Note: API keys (GOOGLE_SHEETS_*, GEMINI_API_KEY) are configured in the backend only.
// The frontend does not need any VITE_* environment variables for API keys.
